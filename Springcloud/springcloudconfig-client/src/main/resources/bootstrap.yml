###需拷贝到每一个模块下

spring:
  # 应用名
  application:
    name: springcloudconfig-client
  # 当前读取配置文件的类型
#  profiles:
#    active: ${spring.profiles.active}
  #配置中心
  cloud:
    config:
      uri: http://localhost:9090/
      profile: prod
server:
  port: 9091